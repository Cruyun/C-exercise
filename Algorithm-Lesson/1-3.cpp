// 设计算法求出数组中相差最小的两个元素（成为最接近数）的差。
// 思路：先排序，再对相邻元素作差，比较出最小的

int func(int a[])
{
	int max = 0;
	int min = 0;
	bool max_flag = false;
	bool min_flag = false;

	for (int i = 0; i < a.size(); i++) {
		
	}
}
