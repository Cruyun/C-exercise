//14．(20分)定义一个含有30个整型元素的数组，按顺序分别赋予从2开始的偶数；然后按顺序每五个数求出一个平均值，放在另一个数组中并输出。试编程。

#include <stdio.h>
int main()
{
    int a[30];
    int b[6];
    int i,s=0;
    for(i=0;i<30;i++)
    {
        a[i]=2*(i+1);
        s+=a[i];
        if(i%5==4)
        {
            b[i/5]=s/5;
            s=0;
        }
    }
    for(i=0;i<6;i++)
        printf("%d ",b[i]);
}
